class Solution(object):
    def removeElement(self, num, val):
        j = 0
        for i in range (len(num)):
            if (num[i] != val):
                num[j] = num[i]
                j= j+1
        return j



        