class Solution:
    def jump(self, nums: List[int]) -> int:
        jump = 0
        end = 0
        reach = 0
        for i in range(len(nums)- 1):
            reach = max(reach, i+nums[i])
            if i == end:
                jump+=1
                end = reach
        return jump        