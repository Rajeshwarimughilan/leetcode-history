class Solution {
    public boolean canJump(int[] nums) {
        int rch = 0;
        for(int i = 0; i < nums.length; i++){
            if(i > rch) return false;
            rch = Math.max(rch, i + nums[i]);
        }
        return true;
    }
}