class Solution(object):
    def canJump(self, nums):
        maxR = 0
        for i in range (0, len(nums)):
            if i >maxR:
                return False
            maxR = max(maxR, i + nums[i]) 
        return True       
        